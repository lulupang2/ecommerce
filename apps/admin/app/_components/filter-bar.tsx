'use client';
import { RotateCcw, Search } from 'lucide-react';
export default function FilterBar({ fields, values, onChange, onReset }) { return <div className="grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-2 xl:grid-cols-5">{fields.map(field => <label key={field.key} className="text-xs font-bold">{field.label}{field.type === 'select' ? <select value={values[field.key] ?? ''} onChange={event => onChange(field.key, event.target.value)} className="mt-2 w-full rounded-lg border border-slate-200 bg-white p-2 text-sm font-normal">{field.options.map(option => <option key={option.value} value={option.value}>{option.label}</option>)}</select> : <div className="relative mt-2"><input type={field.type || 'text'} value={values[field.key] ?? ''} onChange={event => onChange(field.key, event.target.value)} placeholder={field.placeholder} className="w-full rounded-lg border border-slate-200 p-2 pr-8 text-sm font-normal"/><Search size={14} className="pointer-events-none absolute right-3 top-2.5 text-slate-300"/></div>}</label>)}<button type="button" onClick={onReset} className="inline-flex h-9 items-center justify-center gap-2 rounded-lg border border-slate-200 text-xs font-bold text-slate-500 hover:bg-slate-50"><RotateCcw size={13}/> 필터 초기화</button></div>; }
