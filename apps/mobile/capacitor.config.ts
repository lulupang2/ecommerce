import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.techzone.market',
  appName: 'TECHZONE',
  webDir: '../storefront/out',
};

export default config;
