services:
  web:
    ports:
      - "${PUBLIC_BIND_ADDRESS:-127.0.0.1}:15173:8080"

  otel-collector:
    profiles: [observability]

  prometheus:
    profiles: [observability]

  tempo:
    profiles: [observability]

  loki:
    profiles: [observability]

  promtail:
    profiles: [observability]

  grafana:
    profiles: [observability]
