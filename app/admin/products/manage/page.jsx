'use client';
import { useEffect, useState } from 'react';
import { ArrowLeft, Plus, RefreshCw } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { readSession } from '@/lib/session';
const API=process.env.NEXT_PUBLIC_API_BASE_URL||'http://localhost:18080/api'; const won=v=>`${new Intl.NumberFormat('ko-KR').format(v)}원`;
export default function ProductManagementPage(){const[session,setSession]=useState(null);const[products,setProducts]=useState([]);const[loading,setLoading]=useState(true);const[msg,setMsg]=useState('');async function load(){if(!session?.accessToken)return;setLoading(true);const r=await fetch(`${API}/products?status=all`,{headers:{authorization:`Bearer ${session.accessToken}`}});const data=await r.json();if(!r.ok)throw Error('상품 목록을 불러오지 못했습니다.');setProducts(data.items||[]);setLoading(false)}useEffect(()=>{setSession(readSession())},[]);useEffect(()=>{load().catch(e=>{setMsg(e.message);setLoading(false)})},[session]);if(!session||session.user?.role!=='admin')return <main className="grid min-h-screen place-items-center bg-slate-100"><div className="rounded-3xl bg-white p-10 text-center"><h1 className="text-2xl font-black">관리자 로그인이 필요합니다</h1><a href="/login/" className="mt-6 inline-block rounded-xl bg-slate-950 px-5 py-3 text-white">로그인</a></div></main>;return <main className="min-h-screen bg-[#f4f6fa] p-5 text-slate-950 md:p-10"><div className="mx-auto max-w-6xl"><div className="flex items-center justify-between"><a href="/admin/" className="inline-flex items-center gap-2 text-sm font-bold"><ArrowLeft size={16}/> 대시보드</a><div className="flex gap-2"><Button variant="outline" size="sm" onClick={()=>load()}><RefreshCw size={14} className="mr-2"/> 새로고침</Button><a href="/admin/products/"><Button size="sm"><Plus size={15} className="mr-2"/> 상품 등록</Button></a></div></div><header className="py-12"><p className="text-[10px] font-bold tracking-[.24em] text-blue-600">CATALOG MANAGEMENT</p><h1 className="mt-4 text-5xl font-black">상품 목록.</h1><p className="mt-4 text-sm text-slate-500">판매 중인 상품과 노출 상태를 확인하고 등록 화면으로 이동할 수 있습니다.</p></header>{msg&&<p role="alert" className="mb-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{msg}</p>}{loading?<p className="rounded-2xl bg-white p-8">상품을 불러오는 중…</p>:<section className="overflow-x-auto rounded-3xl bg-white p-6"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b text-xs text-slate-400"><tr><th className="pb-3">상품</th><th className="pb-3">브랜드</th><th className="pb-3">카테고리</th><th className="pb-3">가격</th><th className="pb-3">재고</th><th className="pb-3">상태</th><th className="pb-3">관리</th></tr></thead><tbody className="divide-y">{products.map(p=><tr key={p.id}><td className="py-4"><div className="flex items-center gap-3"><img src={p.image} alt="" className="h-12 w-12 rounded-lg object-cover"/><b>{p.name}</b></div></td><td>{p.brand}</td><td>{p.category}</td><td>{won(p.price)}</td><td>{p.stock}</td><td><span className={`rounded-full px-2 py-1 text-xs font-bold ${p.status==='published'?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{p.status}</span></td><td><a href={`/admin/products/?id=${p.id}`} className="font-bold text-blue-600">수정</a></td></tr>)}</tbody></table></section>}</div></main>}
